// import { createMuiTheme } from '@material-ui/core';

export const ProjectSummaryStyles = theme => ({
    tag: {
        background: '#2296f3',
        padding: '3px 8px',
        borderRadius: '20px',
        color: '#fff',
        fontWeight: '700',
        fontSize: '1rem'
    }
});

export const FooterStyles = theme => ({
    footerMain: {
        color: '#fff',
        background: '#cdcdcd'
    },
    footerText: {
        fontSize: '0.8rem'
    }
})